# === Secretos (NUNCA subir) ===
.env
.env.*
!.env.example

# === Datos locales / generados ===
downloads/
# OJO: git no admite comentarios al final de una línea de patrón ("dir/  # texto" no ignora
# nada). Los comentarios van SIEMPRE en su propia línea.
# CSVs derivados del XML de Rekordbox (rutas personales)
ground_truth/out/
# CSVs por corrida del benchmark (rutas personales)
benchmark/out/
# CSVs de la revision de calidad (rutas personales)
calidad/out/
# el export de Rekordbox no va al repo
*.xml
*.db
*.sqlite
*.sqlite3
*.m3u8
.cache

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# === Node / frontend ===
frontend/node_modules/
frontend/dist/
node_modules/
dist/
*.log
npm-debug.log*

# === Editor / OS ===
.vscode/
.idea/
.DS_Store
Thumbs.db

# === Claude Code (memoria/estado local) ===
.claude/
pipeline/staging/

# Salidas de la sesion de ground truth (CSVs con rutas personales, no van al repo)
gt_out/
# Reportes HTML de pipeline.revisar: rutas personales y nombres de la biblioteca (el repo es publico)
pipeline/revision_*.html
# Material de escucha de ground_truth.escucha: fragmentos de audio (cientos de MB) y un
# indice con rutas personales y los veredictos a oido
escucha/
